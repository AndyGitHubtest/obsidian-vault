---
template: dev-data-design
type: dev-project
stage: 3/8
---

# 📊 [项目名称] - 数据设计

> 数据怎么存、怎么流转、怎么保证一致性

## 一、数据库选型
| 数据库 | 用途 | 理由 |
|--------|------|------|
|        |      |      |

## 二、核心表结构

### 表名：`xxx`
| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | 主键 |
| created_at | TIMESTAMP | NOT NULL | NOW() | 创建时间 |
| updated_at | TIMESTAMP | NOT NULL | NOW() | 更新时间 |
| | | | | |

```sql
CREATE TABLE xxx (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);
```

### 索引设计
| 表 | 索引名 | 字段 | 类型 | 说明 |
|----|--------|------|------|------|
|    |        |      |      |      |

## 三、API 接口设计

### 接口清单
| # | 接口 | 方法 | 说明 | 鉴权 |
|---|------|------|------|------|
| 1 | /api/xxx | GET |      | 是/否 |

### 接口详情：`GET /api/xxx`
**请求参数：**
| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
|      |      |      |      |      |

**响应格式：**
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误码：**
| Code | 含义 | 处理 |
|------|------|------|
| 400 | 参数错误 | 检查请求参数 |
| 401 | 未授权 | 重新登录 |
| 500 | 服务器错误 | 联系运维 |

## 四、数据流
```
[描述数据如何流转]

示例:
  用户请求 → API Gateway → 业务逻辑 → 数据库 → 返回响应
```

## 五、数据一致性
- 事务策略：
- 并发控制：乐观锁 / 悲观锁
- 缓存策略：
- 缓存失效机制：

## 六、数据迁移
| 版本 | 变更 | 回滚方案 |
|------|------|----------|
|      |      |          |

## 七、数据安全
- 敏感字段加密：
- 备份策略：
- 数据保留周期：

---
*创建：{{date}} | 作者：Andy | 状态：草稿*
