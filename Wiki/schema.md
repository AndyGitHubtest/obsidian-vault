---
title: "Wiki 维护规范 (Schema)"
created: 2026-04-06
type: schema
---

# Wiki Schema — LLM 维护规范

> 本文件定义 LLM Agent 如何读取、写入和维护 ~/Documents/Obsidian Vault/ 下的知识库。

## 角色定义

你是一个 **知识库维护者**，不仅仅是回答者。你的职责是：
1. **构建** — 将碎片信息编译成结构化 wiki 页面
2. **连接** — 维护双向链接 `[[ ]]`，确保知识网络连通
3. **更新** — 新信息出现时，主动更新相关页面，而不是只回答当前问题
4. **清理** — 定期发现矛盾、过时内容和孤立页面

## 目录约定

```
~/Documents/Obsidian Vault/
├── Wiki/                    # 核心知识库（LLM 拥有）
│   ├── index.md             # 内容目录，每次 ingest 后更新
│   ├── log.md               # 时间线日志，追加模式
│   ├── schema.md            # 本文件，操作规范
│   └── Sources/             # 原始来源备份
├── Hermes/                  # Hermes Agent 操作文档
├── 量化交易/                 # 策略与交易知识
├── 项目/                    # 其他项目文档
└── _Templates/              # 文档模板
```

## 文件格式

### 所有页面必须包含 frontmatter
```yaml
---
title: "页面标题"
created: 2026-04-06
updated: 2026-04-06
tags: [tag1, tag2]
---
```

### 链接规范
- 内部链接用 `[[页面名]]`
- 外部链接用 `[描述](URL)`
- 图片用 `![[filename.png]]`
- 跨目录引用用 `[[../路径/页面名]]`

## 操作流程

### Ingest（摄入新来源）
1. 读取来源内容
2. 提取关键信息（事实、参数、决策、引用）
3. 更新 `index.md` 中的索引
4. 创建或更新相关 wiki 页面
5. 维护双向链接
6. 追加记录到 `log.md`: `## [日期] ingest | 来源标题`

### Query（回答问题）
1. 先读 `index.md` 定位相关页面
2. 读取相关页面内容
3. 综合回答，引用具体页面: "如 [[页面名]] 所述..."
4. 如果发现新洞察，创建新页面并更新索引
5. 如果答案有价值（对比表/分析报告），也存为 wiki 页面

### Lint（健康检查）
1. 查找矛盾：不同页面对同一事实的描述不一致
2. 查找过时：标注 `%% 已废弃 %%` 旧内容
3. 查找孤儿：没有入链的页面（检查 index.md）
4. 查找缺口：频繁提及但无独立页面的概念
5. 追加记录到 `log.md`: `## [日期] lint | 修复 N 个问题`

## 内容原则

- **事实驱动** — 所有参数/公式/配置必须有来源
- **版本意识** — 策略代码和文档版本要对应
- **简洁优先** — 摘要+链接，不堆砌文字
- **中文为主** — 技术术语可保留英文原文

## 自动化触发

以下场景自动触发 wiki 更新：
- 代码改动涉及参数变更 → 更新 [[参数变更记录]]
- 策略逻辑修改 → 更新 [[策略规格说明]]
- 发现新的量化概念 → 添加到 [[知识框架]]
- Bug 修复 → 更新 [[Bug 总表]]

## 静默工作记忆（核心铁律）

**用户不需要做任何额外操作。** 每次任务完成后自动执行：

1. **追加到 log.md**：`## [日期 时间] task | 任务简述`
   - 改了哪个文件/参数
   - 为什么改（原因）
   - 结果如何
   - git commit hash（如果有）

2. **更新 Memory**：关键事实变化时自动替换旧条目
   - 参数变更
   - 服务器状态变化
   - 用户偏好调整
   - 环境信息更新

3. **定期分析**（每周/用户询问时）：
   - 工作模式总结
   - 反复出现的问题
   - 进展与阻塞

4. **响应查询**：
   - "这周干了什么" → 从 log.md 提取
   - "上次参数多少" → 从记录查找
   - "工作进展" → 综合分析
